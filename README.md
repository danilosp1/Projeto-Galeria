# Projeto-Galeria
Projeto de galeria de fotos simples, com filtro por cidade, utilizando Bootstrap, JQuery e Webpack

A execução do projeto é feita:
- "npm start": executa em modo de desenvolvimento (build em cache)
- "npm run build": gera a pasta build
